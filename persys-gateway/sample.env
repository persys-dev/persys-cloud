# Persys Gateway sample environment

# Optional: explicit config path
PERSYS_GATEWAY_CONFIG=./config.yaml

# Optional Vault auth (used when vault.enabled=true in config.yaml)
PERSYS_VAULT_TOKEN=
PERSYS_VAULT_APPROLE_ID=
PERSYS_VAULT_APPROLE_SECRET_ID=
